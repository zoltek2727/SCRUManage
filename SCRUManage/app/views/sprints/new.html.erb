  <div class="row">
        <div class="col s12 m6">
          <div class="card">
            <div class="card-content white-text">
              <span class="card-title">Nowy sprint</span>

<%= form_for @sprint do |f| %>

    <div class="field">
        <%= f.label :sprintName %>
        <%= f.text_field :sprintName, autofocus: true %>
    </div>

    <div class="field">
        <%= f.label :sprintDescription %>
        <%= f.text_field :sprintDescription %>
    </div>

        <%= f.label :sprintDescription %>
        <%= f.collection_select(:id, @sprints, :id, :sprintName, { :prompt => 'Wybierz projekt'}, { class: 'form-control' }) %>

    <div class="actions">
        <%= f.submit "Utwórz", :class => 'waves-effect waves-light btn' %>
    </div>

<% end %>

        </div>
          </div>
        </div>
      </div>
